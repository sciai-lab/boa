defaults:
  - dataset: default

_target_: boa.data.datamodule.ProbeDataModule

split_file: /export/scratch/ialgroup/charge_density/datasplits.json

num_workers: 
  train: 8
  val: 8
  test: 8
batch_size: 
  train: 12
  val: 12
  test: 12

n_probe:
  train: 5000
  val: ${data.n_probe.train}
  test: ${data.n_probe.train}

dataloader_kwargs:
  follow_batch: ["coeffs", "atomic_numbers"]
  list_keys: []