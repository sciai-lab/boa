# @package _global_

defaults:
  - md_default
  - override /data: md
  - override /trainer: default

data:
  basis_info:
    atomic_numbers: [1, 6, 8]
  datamodule:
    dataset:
      mol_name: phenol
