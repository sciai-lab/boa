# @package _global_

defaults:
  - override /data: md
  - override /trainer: default

data:
  basis_info:
    atomic_numbers: [1, 6, 8]
  datamodule:
    dataset:
      mol_name: malonaldehyde

trainer:
  check_val_every_n_epoch: 10