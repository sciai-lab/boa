[tool.pytest.ini_options]
addopts = [
  "--color=yes",
  "--durations=0",
  "--strict-markers",
  "--doctest-modules",
]
filterwarnings = [
  "ignore::DeprecationWarning",
  "ignore::UserWarning",
]
log_cli = "True"
markers = [
  "slow: slow tests",
]
minversion = "6.0"
testpaths = "tests/"

[tool.isort]
known_first_party = ["scdp", "mldft"]

[tool.ruff]
exclude = [
    "scdp/",
    "sciai-dft/",
]
line-length = 99

[tool.ruff.lint]
ignore = ["E741"]
